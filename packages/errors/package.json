{
  "name": "errors",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf .turbo && rm -rf .next && rm -rf dist && rm -rf .pnpm-debug.log"
  },
  "keywords": [],
  "author": "lunacrafts",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "tsconfig": "workspace:*",
    "typescript": "^4.6.3"
  }
}